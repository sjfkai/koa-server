/**
 * 单元测试
 */

const test = require('ava');
// const expect = require('chai').expect;


test('db', async (t) => {
  // const db = require('../../src/common/db'); // eslint-disable-line global-require
  // expect(db).to.be.an('object');
  // expect(db.models).to.be.an('object');
  t.pass();
});

